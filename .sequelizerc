const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "src", "config", "configDatabase.js"),
  "migrations-path": path.resolve(__dirname, "src", "database", "migrations"),
  "models-path": path.resolve(__dirname, "src", "database", "models")
};
